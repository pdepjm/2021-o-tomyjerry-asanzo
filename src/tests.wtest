import example.*

test "Si tom tiene más velocidad puede atrapar" {

	assert.that(tom.puedeAtrapar(nibbles))

}

test "Si tom tiene menos velocidad no puede atrapar" {

	assert.notThat(tom.puedeAtrapar(jerry))

}